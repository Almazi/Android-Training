<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="almazi.calculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test1" />
        <activity android:name=".test2" />
        <activity android:name=".Output" />
        <activity android:name=".first_row" />
        <activity android:name=".second_row" />
        <activity android:name=".third_row" />
        <activity android:name=".fourth_row" />
        <activity android:name=".fifth_row"></activity>
    </application>

</manifest>